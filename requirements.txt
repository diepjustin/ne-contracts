requests==2.31.0
beautifulsoup4==4.12.3
pypdf==6.14.2
# pypdf needs this to open AES-encrypted PDFs and raises rather than saying so
# plainly. Without it 729 purchase orders failed as "errors" and looked like
# malformed documents; they open fine once it is installed.
cryptography>=3.1

# A second opinion for PDFs pypdf refuses to open. 3,181 documents fail on a
# single "Invalid object in /Pages" and render perfectly well; pdfminer
# reconstructs more and opened 12 of 12 in a sample. Fallback only.
pdfminer.six>=20221105

# Test suite (scripts/../tests). No network, no data/ -- runs in CI before
# the scrape so a broken parser stops the run early.
pytest>=7.0
