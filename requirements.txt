requests==2.31.0
beautifulsoup4==4.12.3
pypdf==6.14.2
# pypdf needs this to open AES-encrypted PDFs and raises rather than saying so
# plainly. Without it 729 purchase orders failed as "errors" and looked like
# malformed documents; they open fine once it is installed.
cryptography>=3.1
